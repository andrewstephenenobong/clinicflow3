Continue working on the existing ClinicFlow React + TypeScript + Vite + Tailwind frontend.

This is strictly a UI/UX refinement task. Do not modify backend logic, API structure, or data flow. Do not introduce new features or remove existing functionality.

CRITICAL UI FIX REQUIREMENTS
1. REMOVE ALL EMOJIS FROM UI

Scan the entire frontend and remove every emoji from:

headings
buttons
empty states
badges
system messages
sidebar labels
alerts

Replace emojis with clean text or proper icon components (lucide-react or equivalent).

No emoji usage is allowed anywhere in production UI.

2. FIX EMPTY STATES COPY

Replace all informal or friendly text with professional hospital-grade wording.

Examples:

“No patients waiting. Quiet moment” → “No active patients in queue”
“Quiet moment” → remove entirely
Any casual wording must be replaced with neutral clinical phrasing

Keep all messages short, clear, and professional.

3. STANDARDIZE UI LANGUAGE

Ensure consistent casing and tone across the entire app:

Use Title Case for buttons and labels
Use neutral, clinical language everywhere
Avoid conversational tone completely

Example:

“Check in patient” → “Check In Patient”
“Emergency” → plain badge without emoji
4. ICON POLICY
Use proper UI icons (lucide-react or existing icon system)
Do not use emojis as icons
Ensure visual consistency across sidebar, buttons, and badges
5. PRESERVE CURRENT LAYOUT

Do NOT:

change layout structure
redesign pages
modify backend calls
change API responses
alter routing system

Only modify UI text, icons, and minor frontend presentation.

6. UI TONE TARGET

The final UI should feel like a professional hospital operations system used in real clinics, not a demo or startup prototype.

Tone should be:

clean
minimal
clinical
precise

Not:

friendly
playful
AI-generated
7. FINAL CHECK BEFORE COMPLETION

Before finishing, verify:

no emojis remain anywhere in UI
no casual language remains
all pages still function correctly
no API or backend integration is broken
layout remains unchanged

If any backend change seems required, do not implement it. Leave a comment explaining the issue instead.